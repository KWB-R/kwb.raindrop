% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_scenarios.R
\name{run_scenarios}
\alias{run_scenarios}
\title{Run scenarios (parallel or sequential) with a user-supplied worker function}
\usage{
run_scenarios(
  indices,
  run_one_scenario,
  timestep_hours,
  debug = FALSE,
  ...,
  parallel = TRUE,
  workers = parallel::detectCores(),
  show_progress = TRUE,
  progress_handler = "txtprogressbar"
)
}
\arguments{
\item{indices}{Vector. Scenario identifiers to iterate over (often integer
row indices).}

\item{run_one_scenario}{Function. Worker function with signature
\code{function(x, timestep_hours, debug, ...)}. Must accept the arguments
\code{timestep_hours} and \code{debug}.}

\item{timestep_hours}{Numeric. Time step (hours) forwarded to
\code{run_one_scenario}.}

\item{debug}{Logical. Debug flag forwarded to \code{run_one_scenario}.}

\item{...}{Additional arguments forwarded to \code{run_one_scenario}.}

\item{parallel}{Logical. If \code{TRUE}, use \code{future.apply::future_lapply}.
If \code{FALSE}, use base \code{lapply}.}

\item{workers}{Integer. Number of workers when \code{parallel = TRUE}.
Defaults to \code{parallel::detectCores()}.}

\item{show_progress}{Logical. If \code{TRUE}, show progress.}

\item{progress_handler}{Character. Progress handler key. One of
\code{"txtprogressbar"}, \code{"rstudio"}, \code{"cli"}.}
}
\value{
A list with one element per \code{indices} entry containing the
return values of \code{run_one_scenario}.
}
\description{
Executes scenarios by applying \code{run_one_scenario} to each element of
\code{indices}. Supports parallel execution via \pkg{future.apply} and
sequential execution for debugging. Optionally shows progress via
\pkg{progressr}.
}
