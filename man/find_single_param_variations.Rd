% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_single_param_variations.R
\name{find_single_param_variations}
\alias{find_single_param_variations}
\title{Find scenarios that differ from a reference in exactly one parameter}
\usage{
find_single_param_variations(
  data,
  ref_scenario = "s00001",
  id_col = "scenario_name",
  param_cols = NULL,
  exclude_cols = NULL,
  exclude_cols_regex = "^h_",
  tol = 1e-09,
  quiet = FALSE,
  include_reference = c("per_param", "none")
)
}
\arguments{
\item{data}{A data frame with scenarios.}

\item{ref_scenario}{Reference scenario ID (default \code{"s00001"}).}

\item{id_col}{Name of the scenario ID column (default \code{"scenario_name"}).}

\item{param_cols}{Optional character vector of parameter columns. If \code{NULL},
parameters are inferred.}

\item{exclude_cols}{Character vector to exclude from parameter detection.}

\item{exclude_cols_regex}{Regex to auto-exclude non-parameter columns (default \code{"^h_"}).}

\item{tol}{Numeric tolerance for numeric comparisons (default \code{1e-9}).}

\item{quiet}{Logical; print diagnostics if \code{FALSE} (default).}

\item{include_reference}{\code{"per_param"} (default) to include one reference row
per parameter with hits, or \code{"none"}.}
}
\value{
A data frame with columns: \code{id_col}, \code{param_name}, \code{param_value},
\code{is_reference}. Attributes: \code{"param_cols"}, \code{"ref_row"}, \code{"diff_mat"}, \code{"n_diff"}.
}
\description{
Identifies rows that differ from a reference in \emph{exactly one} parameter.
You may pass \code{param_cols} explicitly, or let the function infer them by
excluding \code{id_col}, \code{exclude_cols}, and any columns matching
\code{exclude_cols_regex} (default excludes result columns like \verb{h_*}).
}
